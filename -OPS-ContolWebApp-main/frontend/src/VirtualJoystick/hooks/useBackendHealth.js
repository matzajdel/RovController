/**
 * Re-export from shared hooks directory.
 * This file exists only for backward-compatibility so that existing
 * imports like `from "./hooks/useBackendHealth"` keep working.
 */
export { useBackendHealth } from "../../hooks/useBackendHealth";
